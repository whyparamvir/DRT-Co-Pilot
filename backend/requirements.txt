# Web API
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.8

# Numerics
pandas>=2.2
numpy>=1.26
scipy>=1.12

# DRT engine (Ciucci-lab pyDRTtools).
# NOTE: pyDRTtools 0.2 under-declares its dependencies (it lists only click/ipython) but
# imports PyQt5, cvxopt, scikit-learn and matplotlib at import time. We pin the version we
# test against and list those packages explicitly so a fresh install actually runs.
pyDRTtools==0.2
cvxopt>=1.3
scikit-learn>=1.3
matplotlib>=3.8
PyQt5>=5.15

# Tests
pytest>=8.2
httpx>=0.27
